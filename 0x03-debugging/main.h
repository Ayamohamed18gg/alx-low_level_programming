#include "main.h"
/**
 * main - Tests function that prints if integer in positive or negative
 *
 * Return: 0
 */
int main(void)
{
	int i;


	i = 0;
	positive_or_negative(i);
int largest_number(int a, int b, int c);

	return (0);
}
